========================================================================
    DYNAMIC LINK LIBRARY : killmsg Project Overview
========================================================================

This is the killmsg. It shows the message "you killed tanknamehere" every time you gain a kill.

It takes one parameter only: Set to 0 if you have -tk option, set to 1 if you don't.

